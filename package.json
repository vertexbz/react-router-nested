{
  "name": "react-router-nested",
  "version": "1.0.0",
  "description": "React Router for nesting in for declaring nested paths without knowledge of what was the parent url for current scope.",
  "main": "lib/index.js",
  "author": "Adam Makświej",
  "license": "MIT",
  "dependencies": {
    "history": "^4.7.2",
    "react": "^16.2.0",
    "react-router": "^4.2.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-class-properties": "^6.24.1",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-flow": "^6.23.0",
    "babel-preset-latest": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "flow": "^0.2.3",
    "flow-bin": "^0.61.0"
  },
  "scripts": {
    "build:flow": "flow gen-flow-files src --out-dir lib",
    "build:src": "babel src --out-dir lib -s",
    "build": "yarn build:src"
  }
}
